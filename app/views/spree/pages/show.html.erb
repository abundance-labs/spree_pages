<%= render partial: 'og_meta_tags', locals: { page: @page } %>

<% cache([:v3, @page]) do %>
  <center><h2><%= @page.title %></h2></center>
  <br>
  <div id='page_content'>
    <%= raw @page.content %>
  </div>
<% end %>
