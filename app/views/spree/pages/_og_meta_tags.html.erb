<% content_for(:head) do %>
  <% page_meta_title = page.meta_title.presence || @title %>
  <meta name="title" content="<%= page_meta_title %>">
  <meta name="og:title" content="<%= page_meta_title %>">
  <meta name="og:description" content="<%= page.meta_description || meta_data[:description] %>">
  <% if page.meta_image.exists? %>
    <% Spree::Page.meta_image_sizes.keys.each do |style| %>
      <meta name="og:image" content="<%= page.meta_image.url(style) %>">
      <meta name="og:image:width" content="<%= Spree::Page.meta_image_sizes_tags[style][:width] %>">
      <meta name="og:image:height" content="<%= Spree::Page.meta_image_sizes_tags[style][:height] %>">
    <% end %>
  <% end %>
<% end %>
